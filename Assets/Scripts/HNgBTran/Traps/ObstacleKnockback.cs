using UnityEngine;

// Gắn component này vào obstacle nào cần lực bắn (knockback) khác với mặc định của ObstacleHandler.
public class ObstacleKnockback : MonoBehaviour
{
    public float knockbackForce = 10f;
}